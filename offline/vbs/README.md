# Compatibility package

The canonical V3C/VBS local-first implementation moved to `offline/corpus/` and `offline/config.py`.
This package re-exports the earlier `offline.vbs.*` names so existing scripts do not break during the paper run.
