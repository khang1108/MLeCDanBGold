# GIFs Folder

Place any `.gif`, `.webp`, `.png`, or `.jpg` animation files inside this directory.
The application will automatically detect all GIF/image files in this folder and loop through them during query search wait times.
